[package]
name = "fusion-api-internal"
version = "0.14.0"
edition = "2021"

[dependencies]
fusion-core = { path = "../fusion-core" }
fusion-kernel = { path = "../fusion-kernel" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
